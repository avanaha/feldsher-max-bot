{
  "name": "feldsher-max-bot",
  "version": "1.0.0",
  "description": "MAX Messenger bot for Feldsher.Ryadom project",
  "main": "bot.ts",
  "scripts": {
    "start": "bun run bot.ts",
    "dev": "bun --watch run bot.ts"
  },
  "dependencies": {
    "@maxhub/max-bot-api": "^1.0.0",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "typescript": "^5"
  }
}
